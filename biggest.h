#ifndef BIGGEST_H
#define BIGGEST_H
#define CNT 100
#define MININFI -100000000000000000
#include <stdio.h>
#include <stdlib.h>
long long b_sum(const int* a, size_t n);
size_t b_maxsum_num(const int (*a)[CNT], size_t n, size_t m);
#endif // BIGGEST_H
